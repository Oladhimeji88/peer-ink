name: device_security
description: Trust, secure token, secure storage, and certificate pinning utilities for SNAPLINK.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  core_protocol:
    path: ../core_protocol
  crypto: ^3.0.3
  flutter_secure_storage: ^9.2.2
  isar: ^3.1.0+1
  meta: ^1.12.0

dev_dependencies:
  build_runner: ^2.4.9
  isar_generator: ^3.1.0+1
  mocktail: ^1.0.3
  test: ^1.25.2

