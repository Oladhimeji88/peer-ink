name: local_discovery
description: Local Wi-Fi and fallback discovery abstractions for SNAPLINK trusted reconnect.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  core_protocol:
    path: ../core_protocol
  meta: ^1.12.0
  multicast_dns: ^0.3.2+6

dev_dependencies:
  mocktail: ^1.0.3
  test: ^1.25.2

